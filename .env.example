# remove.bg API key for the "Remove BG" image action.
# Get it at https://www.remove.bg/dashboard#api-key
# Free plan = 50 background removals/month (resets on the 1st).
# Copy this file to .env.local for local dev; set the same var on the VPS for prod.
REMOVE_BG_API_KEY=
